// this is context Api
import { createContext } from "react";
// context hold all states, that related to notes
// it is simple

const noteContext = createContext();

export default noteContext
// we tell to ract that we want tot use the context api